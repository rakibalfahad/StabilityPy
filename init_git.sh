#!/bin/bash

# Initialize git repository
git init

# Add all files
git add .

# Commit the changes
git commit -m "Initial commit: Modernized stability selection with GPU acceleration"

echo "Git repository initialized with all files."
echo "Next steps:"
echo "1. Set your remote repository: git remote add origin <your-repo-url>"
echo "2. Push to remote: git push -u origin master"
